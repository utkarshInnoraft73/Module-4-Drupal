/**
 * @file
 * sub-bootstrap behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.subBootstrap = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
