/**
 * @file
 * Custom theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.customTheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
